main () {
  const myexp = 1e2;
  int toto, tofu=-3;
  int foo, bar, baz;

  printf(myexp);
  *(&foo) = *(&tofu);
  printf(foo);
  printf(toto);
  toto = toto + -1;
  printf(toto);
  bar = -1e1;
  foo = toto;
}
